// The Observer pattern defines an object (called subject) that can notify
// a set of observers (or listeners) when a change in its state occurs.
// The Observer pattern is already built into the core and is available through the EventEmitter class.
// I describe it in ../Module/EventEmitter